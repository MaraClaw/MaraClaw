# Dependencies & build output
node_modules
dist
dist-ssr

# Local env / secrets (public VITE_* pass via --build-arg only)
.env
.env.*
!.env.example

# Logs & OS noise
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store
*.local
*.log

# Editor / VCS
.git
.gitignore
.vscode
.idea
*.swp
*.swo

# Docs not needed in the image context (optional keep for layer size)
# README.md AGENTS.md stay out of COPY list already; ignore bulk noise
coverage
*.tsbuildinfo
